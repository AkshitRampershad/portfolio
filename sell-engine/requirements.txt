# The prototype runs with no dependencies at all: the heuristic reasoner is
# stdlib-only, and that is the default.
#
# Install this only to use --reasoner claude, which hands hypothesis generation
# to the model and so handles drift shapes the heuristics were never taught.
# It also needs ANTHROPIC_API_KEY (or an `ant auth login` profile) in the
# environment; without one the agent falls back to heuristics and says so.
anthropic>=1.0.0
